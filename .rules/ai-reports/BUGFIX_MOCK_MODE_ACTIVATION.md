# Отчёт: Исправление активации MOCK режима в AI сервисе

**Дата**: 25 декабря 2025  
**Тип задачи**: Исправление бага  
**Статус**: ✅ Завершено

---

## 🎯 Проблема

Mock режим для AI парсинга не активировался, даже когда в `.env` файле был установлен флаг `OPENAI_MOCK_MODE=true`. Метод `mockParseAndCreate` был реализован, но никогда не вызывался.

### Причина

В методе `parseAndCreateTransactions` отсутствовала проверка флага `OPENAI_MOCK_MODE` и условный вызов mock-метода.

---

## ✅ Решение

### 1. Добавлена проверка флага MOCK режима

**Файл**: `apps/backend/src/modules/ai/ai.service.ts`

```typescript
async parseAndCreateTransactions(text: string, accountId?: string) {
  // Проверяем флаг MOCK режима
  const isMockMode = this.configService.get<string>('OPENAI_MOCK_MODE') === 'true';
  if (isMockMode) {
    console.log('🧪 MOCK MODE ENABLED - using mockParseAndCreate');
    return this.mockParseAndCreate(text, accountId);
  }
  
  // ... остальной код для реального OpenAI API
}
```

**Изменения**:
- ✅ Добавлена проверка переменной окружения `OPENAI_MOCK_MODE`
- ✅ При значении `'true'` вызывается `mockParseAndCreate` вместо реального API
- ✅ Добавлен console.log для подтверждения активации mock режима

### 2. Создан тестовый скрипт

**Файл**: `test-mock-mode.sh`

```bash
#!/bin/bash

echo "🧪 Testing MOCK MODE"
echo "===================="

RESPONSE=$(curl -s -X POST http://localhost:3001/api/ai/parse \
  -H "Content-Type: application/json" \
  -d '{"text":"купил кофе 25 и круассан 15"}')

echo "$RESPONSE" | python3 -c "import sys, json; print(json.dumps(json.loads(sys.stdin.read()), indent=2, ensure_ascii=False))"

echo ""
echo "✅ Check backend logs for '🧪 MOCK MODE ENABLED' message"
```

---

## 🧪 Тестирование

### Настройка

```bash
# apps/backend/.env
OPENAI_MOCK_MODE=true
```

### Результат тестирования

```bash
$ ./test-mock-mode.sh

🧪 Testing MOCK MODE
====================

📤 Sending test request to AI parser...

{
  "success": true,
  "transactions": [
    {
      "amount": 25,
      "type": "expense",
      "categoryId": "694d154b3f196a9b3f42a4f7",
      "accountId": "694d154c3f196a9b3f42a506",
      "description": "MOCK: купил кофе 25 и круассан 15",
      "date": "2025-12-25T11:45:02.064Z",
      "_id": "694d23beae3eadf2cdf23535",
      ...
    },
    {
      "amount": 15,
      ...
    }
  ],
  "message": "🧪 MOCK: Создано 2 транзакций (тестовый режим)"
}
```

### ✅ Проверка функциональности

- ✅ Mock режим активируется при `OPENAI_MOCK_MODE=true`
- ✅ Числа корректно извлекаются из текста (25 и 15)
- ✅ Создаются транзакции для каждого числа
- ✅ Сообщение содержит метку "🧪 MOCK" для идентификации режима
- ✅ Не происходит реальных вызовов к OpenAI API

---

## 📝 Как работает Mock режим

### Логика `mockParseAndCreate`

1. **Извлечение чисел**: Ищет все числа в тексте через regex `/\d+/g`
2. **Валидация**: Проверяет, что сумма в разумных пределах (0 < amount < 1000000)
3. **Создание транзакций**: Для каждого числа создаёт expense транзакцию
4. **Fallback**: Если чисел не найдено, создаёт транзакцию с дефолтной суммой 10
5. **Метка**: Добавляет префикс "MOCK:" к описанию для идентификации

### Преимущества

- 🚀 **Быстрое тестирование** без траты OpenAI квоты
- 🔧 **Разработка offline** без необходимости в API ключе
- 💰 **Экономия средств** на тестовых запросах
- 🧪 **Предсказуемость** для unit-тестов

---

## 🔄 Переключение режимов

### Включить MOCK режим

```bash
# apps/backend/.env
OPENAI_MOCK_MODE=true
```

Перезапустить backend:
```bash
yarn workspace @ai-finance/backend dev
```

### Включить реальный AI

```bash
# apps/backend/.env
OPENAI_MOCK_MODE=false
# или удалить строку
```

⚠️ **Важно**: Изменения в `.env` требуют перезапуска backend сервера!

---

## 📚 Связанные файлы

- `apps/backend/src/modules/ai/ai.service.ts` - AI сервис с mock логикой
- `apps/backend/.env` - конфигурация флага `OPENAI_MOCK_MODE`
- `test-mock-mode.sh` - тестовый скрипт для проверки mock режима

---

## 🎓 Выводы

1. ✅ **Проблема решена**: Mock режим теперь корректно активируется
2. ✅ **Добавлен тест**: Скрипт для быстрой проверки mock режима
3. ✅ **Улучшена отладка**: Console.log сообщения для диагностики
4. 📝 **Документация**: Создан отчёт с инструкциями по использованию

### Рекомендации

- Использовать mock режим для локальной разработки
- Переключаться на реальный AI только для финального тестирования
- Мониторить консоль backend на наличие "🧪 MOCK MODE ENABLED"

---

**Автор**: GitHub Copilot  
**Версия**: 1.0.0

