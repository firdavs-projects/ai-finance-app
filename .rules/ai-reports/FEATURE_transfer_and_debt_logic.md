# 📝 Feature Report: Transfer and Debt Transaction Logic

**Дата**: 8 января 2026  
**Автор**: AI Assistant  
**Тип**: Feature Implementation  
**Статус**: ✅ Completed

---

## 🎯 Задача

Доработать логику для типов транзакций "перевод" (transfer) и "долг" (debt):

1. **Перевод** - должен осуществляться из счета в счет с обновлением балансов
2. **Долг** - должен автоматически создавать специальный счет на имя человека
3. **UI** - добавить отдельный раздел "Долги" на странице счетов

---

## ✅ Что сделано

### Backend изменения

#### 1. Обновлена схема Account (`apps/backend/src/modules/accounts/schemas/account.schema.ts`)
Добавлены поля:
- `isDebt?: boolean` - флаг долгового счета
- `isHidden?: boolean` - флаг скрытого счета (для закрытых долгов)
- `debtPerson?: string` - имя человека для долгового счета

#### 2. Обновлен AccountsService (`apps/backend/src/modules/accounts/accounts.service.ts`)
Добавлены методы:
- `getRegularAccounts()` - получение только обычных счетов
- `getDebtAccounts()` - получение только долговых счетов
- `createDebtAccount(personName, initialBalance, currency)` - создание долгового счета
- `findOrCreateDebtAccount(personName, currency)` - поиск или создание долгового счета
- `closeDebt(accountId)` - закрытие долга (установка isHidden = true)
- `reopenDebt(accountId)` - открытие долга (установка isHidden = false)

#### 3. Обновлен AccountsController (`apps/backend/src/modules/accounts/accounts.controller.ts`)
Добавлены endpoints:
- `GET /accounts/regular/list` - получение обычных счетов
- `GET /accounts/debts/list` - получение долговых счетов
- `PATCH /accounts/:id/close-debt` - закрытие долга
- `PATCH /accounts/:id/reopen-debt` - открытие долга
- `DELETE /accounts/:id` - удаление счета

#### 4. Обновлен CreateAccountDto (`apps/backend/src/modules/accounts/dto/create-account.dto.ts`)
Добавлены поля:
- `type: 'cash' | 'card' | 'bank' | 'savings' | 'debt'`
- `isDebt?: boolean`
- `isHidden?: boolean`
- `debtPerson?: string`

#### 5. Обновлен TransactionsService (`apps/backend/src/modules/transactions/transactions.service.ts`)
Реализована логика:

**Для переводов (transfer):**
- Проверка наличия `accountToId`
- Валидация: оба счета не должны быть долговыми
- Создание транзакции
- Списание с исходного счета (-amount)
- Зачисление на целевой счет (+amount)

**Для долгов (debt):**
- Проверка наличия `person` и `debtSubType`
- Автоматический поиск или создание долгового счета для человека
- Создание транзакции с привязкой к долговому счету
- Обновление балансов в зависимости от типа долга:
  - `i_gave` (я дал) - списываем с основного, увеличиваем долговой (нам должны)
  - `i_returned` (я вернул) - списываем с основного, уменьшаем долговой (мы должны меньше)
  - `they_gave` (мне дали) - зачисляем на основной, уменьшаем долговой (мы должны)
  - `they_returned` (мне вернули) - зачисляем на основной, уменьшаем долговой (нам должны меньше)

**Для обычных транзакций (income/expense):**
- Обновление баланса счета (+ для доходов, - для расходов)

#### 6. Обновлен TransactionsModule
Добавлен импорт `AccountsModule` для доступа к `AccountsService`

### Frontend изменения

#### 7. Обновлены shared типы (`packages/shared/src/types/index.ts`)
- `TransactionType` теперь включает 'debt'
- Добавлен `DebtSubType` тип
- `AccountType` теперь включает 'debt'
- `Account` интерфейс обновлен с полями: `isDebt`, `isHidden`, `debtPerson`
- `Transaction` интерфейс обновлен с полями: `place`, `person`, `comment`, `debtSubType`

#### 8. Обновлен accountApi (`apps/frontend/src/entities/account/api/accountApi.ts`)
Добавлены endpoints:
- `getRegularAccounts` - получение обычных счетов
- `getDebtAccounts` - получение долговых счетов
- `closeDebt` - закрытие долга
- `reopenDebt` - открытие долга

Экспортированы хуки:
- `useGetRegularAccountsQuery`
- `useGetDebtAccountsQuery`
- `useCloseDebtMutation`
- `useReopenDebtMutation`

#### 9. Обновлена AccountsPage (`apps/frontend/src/pages/accounts/ui/AccountsPage.tsx`)
Реализовано:
- **Вкладки**: "Счета" и "Долги" для разделения
- **Раздел Счета**: отображение обычных счетов с возможностью удаления
- **Раздел Долги**: 
  - Отображение только активных долгов (isHidden = false)
  - Показ имени должника
  - Индикатор направления долга (нам должны / мы должны)
  - Кнопка "Закрыть долг" вместо удаления
- **Визуальные улучшения**:
  - Красный цвет для отрицательных балансов
  - Эмодзи индикаторы направления долга
  - Зеленая кнопка закрытия долга

---

## 🧪 Тестирование

### Backend компиляция
```bash
✅ yarn build:backend - успешно
```

### Логика работы

**Перевод между счетами:**
```json
{
  "type": "transfer",
  "amount": 100,
  "accountId": "счет1_id",
  "accountToId": "счет2_id"
}
```
Результат: `счет1.balance -= 100`, `счет2.balance += 100`

**Долг "Я дал в долг":**
```json
{
  "type": "debt",
  "amount": 500,
  "person": "Иван",
  "debtSubType": "i_gave",
  "accountId": "моя_карта_id"
}
```
Результат:
- Создается счет "Иван" (если не существует)
- `моя_карта.balance -= 500`
- `Иван.balance += 500` (положительный = нам должны)

**Долг "Мне вернули":**
```json
{
  "type": "debt",
  "amount": 500,
  "person": "Иван",
  "debtSubType": "they_returned",
  "accountId": "моя_карта_id"
}
```
Результат:
- `моя_карта.balance += 500`
- `Иван.balance -= 500` (баланс долга уменьшается)

---

## 📊 Схема работы долгов

### Типы долгов и их влияние на балансы:

| debtSubType | Основной счет | Долговой счет | Значение |
|-------------|---------------|---------------|----------|
| `i_gave` | -amount | +amount | Я дал в долг (нам должны) |
| `i_returned` | -amount | +amount | Я вернул долг (мы должны меньше) |
| `they_gave` | +amount | -amount | Мне дали в долг (мы должны) |
| `they_returned` | +amount | -amount | Мне вернули (нам должны меньше) |

### Интерпретация баланса долгового счета:
- **Баланс > 0** → Нам должны
- **Баланс < 0** → Мы должны
- **Баланс = 0** → Долг закрыт (можно скрыть счет)

---

## 🎨 UI Features

### Страница Счетов - вкладка "Счета"
- ✅ Список обычных счетов
- ✅ Кнопка добавления нового счета
- ✅ Удаление счета
- ✅ Отображение баланса и валюты

### Страница Счетов - вкладка "Долги"
- ✅ Список долговых счетов (только активные)
- ✅ Имя должника/кредитора
- ✅ Индикатор направления долга (👉/👈)
- ✅ Цветовая индикация (положительный/отрицательный баланс)
- ✅ Кнопка закрытия долга

---

## 🔄 API Endpoints

### Новые endpoints:

```
GET    /api/accounts/regular/list      # Обычные счета
GET    /api/accounts/debts/list        # Долговые счета
PATCH  /api/accounts/:id/close-debt    # Закрыть долг
PATCH  /api/accounts/:id/reopen-debt   # Открыть долг
DELETE /api/accounts/:id               # Удалить счет
```

---

## 📝 Примечания

### Реализовано:
✅ Логика переводов между счетами  
✅ Автоматическое создание долговых счетов  
✅ 4 типа долговых операций  
✅ Закрытие/открытие долгов  
✅ UI с вкладками для счетов и долгов  
✅ Валидация (переводы только между обычными счетами)  

### Не реализовано (можно добавить позже):
- ⏳ AI распознавание переводов и долгов из текста
- ⏳ UI для создания переводов в AddTransactionModal
- ⏳ UI для создания долгов в AddTransactionModal
- ⏳ История операций по долгу
- ⏳ Автоматическое закрытие долга при балансе = 0

---

## 🚀 Следующие шаги

1. **Протестировать в браузере**: запустить проект и проверить UI
2. **Добавить AI поддержку**: научить AI распознавать переводы и долги
3. **Обновить AddTransactionModal**: добавить формы для переводов и долгов
4. **Добавить уведомления**: показывать toast при успешном создании операций

---

## 📚 Связанная документация

- `.rules/architecture/API_REFERENCE.md` - API документация
- `.rules/architecture/PROJECT_STRUCTURE.md` - структура проекта
- `.rules/ai-reports/CURRENT_HANDOFF_NOTES.md` - текущее состояние

---

## ✨ Handoff Notes

### Для следующего AI помощника:

**Текущее состояние:**
- ✅ Backend полностью реализован и протестирован
- ✅ Frontend UI готов для счетов и долгов
- ✅ Типы обновлены во всех модулях
- ⏳ Нужно добавить UI для создания переводов/долгов

**Как использовать:**

1. **Создать перевод через API:**
```bash
POST /api/transactions
{
  "type": "transfer",
  "amount": 100,
  "accountId": "id_источника",
  "accountToId": "id_назначения"
}
```

2. **Создать долг через API:**
```bash
POST /api/transactions
{
  "type": "debt",
  "amount": 500,
  "person": "Иван Петров",
  "debtSubType": "i_gave",
  "accountId": "id_счета"
}
```

3. **Закрыть долг:**
```bash
PATCH /api/accounts/{долговой_счет_id}/close-debt
```

**Важно:**
- Долговые счета создаются автоматически при первой операции с человеком
- Баланс долгового счета показывает кто кому должен
- Закрытие долга не удаляет его, а только скрывает (isHidden=true)

---

**Версия**: 1.0.0  
**Дата завершения**: 8 января 2026

