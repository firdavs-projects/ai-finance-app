# 📋 AI Instructions - Инструкции для AI помощников

## 🚨 НАЧНИ ЗДЕСЬ: Инициализация сессии

**ПЕРЕД началом работы** прочитай файл инициализации:

👉 **[AI_SESSION_INIT.md](../AI_SESSION_INIT.md)** - обязательный чек-лист для каждой сессии

Этот файл содержит:
- ✅ Чек-лист обязательных файлов для чтения
- ✅ Порядок инициализации
- ✅ Быстрые команды
- ✅ Примеры сценариев работы

---

## 🎯 ВАЖНОЕ ПРАВИЛО: Расположение документации

**ВСЕГДА** храни и обновляй документацию ТОЛЬКО в папке `.rules/`

### Структура документации (КАТЕГОРИЗИРОВАННАЯ):

```
.rules/
├── INDEX.md                    # Главная навигация
│
├── 📘 business/                # БИЗНЕС-ДОКУМЕНТАЦИЯ
│   ├── GETTING_STARTED.md      # Руководство по началу работы
│   └── README.md               # Общее описание проекта
│
├── 🏗️ architecture/            # АРХИТЕКТУРА И ЛОГИКА
│   ├── AI_INSTRUCTIONS.md      # Этот файл - инструкции для AI
│   ├── PROJECT_STRUCTURE.md    # Детальная структура проекта
│   └── API_REFERENCE.md        # Справочник API endpoints
│
└── 📋 ai-reports/              # ОТЧЁТЫ AI ПОМОЩНИКОВ
    ├── BACKEND_STATUS.md       # Отчёт о проверке бэкенда
    ├── YARN_MIGRATION.md       # Отчёт о миграции на Yarn
    └── DOCUMENTATION_MIGRATION.md  # Отчёт о реорганизации
```

### ✅ Правила работы с документацией:

1. **Бизнес-документация** (`business/`):
   - ✅ Создавай руководства для пользователей
   - ✅ Обновляй при добавлении новых фич
   - ❌ НЕ добавляй технические детали реализации

2. **Архитектура** (`architecture/`):
   - ✅ Документируй структуру проекта
   - ✅ Обновляй при изменении архитектуры
   - ✅ Описывай принципы и паттерны
   - ❌ НЕ добавляй отчёты о выполненных задачах

3. **Отчёты AI** (`ai-reports/`):
   - ✅ Создавай отчёты о выполненных задачах
   - ✅ Документируй миграции и изменения
   - ✅ Фиксируй исправленные проблемы
   - ❌ НЕ редактируй старые отчёты (только добавляй новые)

4. **Корневой README.md**:
   - ✅ Держи краткую версию с ссылками на `.rules/`
   - ✅ Основное содержание должно быть в `.rules/`

### 📝 Типы документации по категориям:

**business/** (Бизнес):
- GETTING_STARTED.md - Установка, настройка, запуск
- README.md - Общее описание проекта
- FAQ.md - Часто задаваемые вопросы (создай при необходимости)
- USER_GUIDE.md - Руководство пользователя (создай при необходимости)

**architecture/** (Архитектура):
- AI_INSTRUCTIONS.md - Этот файл с инструкциями для AI
- PROJECT_STRUCTURE.md - Детальная структура монорепозитория
- API_REFERENCE.md - Справочник API endpoints
- TECH_STACK.md - Технологии (создай при необходимости)
- DEPLOYMENT.md - Развёртывание (создай при необходимости)

**ai-reports/** (Отчёты AI):
- BACKEND_STATUS.md - Отчёт о проверке бэкенда
- YARN_MIGRATION.md - Отчёт о миграции на Yarn
- DOCUMENTATION_MIGRATION.md - Отчёт о реорганизации
- FEATURE_*.md - Отчёты о новых фичах (создавай при добавлении)
- BUGFIX_*.md - Отчёты об исправлениях (создавай при фиксах)

### 🔄 При добавлении нового функционала:

1. Обнови `architecture/PROJECT_STRUCTURE.md` (если изменилась структура)
2. Обнови `architecture/API_REFERENCE.md` (если добавились endpoints)
3. Обнови `business/GETTING_STARTED.md` (если изменился процесс запуска)
4. Создай отчёт в `ai-reports/FEATURE_название.md`
5. Обнови `.rules/INDEX.md` (если добавились новые файлы)

### 💡 Примеры:

**✅ ПРАВИЛЬНО:**
```bash
# Бизнес-документация
.rules/business/GETTING_STARTED.md
.rules/business/FAQ.md

# Архитектура
.rules/architecture/PROJECT_STRUCTURE.md
.rules/architecture/DEPLOYMENT.md

# Отчёты AI
.rules/ai-reports/BACKEND_STATUS.md
.rules/ai-reports/FEATURE_auth_implementation.md
.rules/ai-reports/BUGFIX_transaction_validation.md
```

**❌ НЕПРАВИЛЬНО:**
```bash
# НЕ создавай документацию в других местах!
docs/
DEPLOYMENT.md  # в корне
apps/frontend/README.md  # в подпроектах
```

---

## 🏗️ Структура проекта AI Finance

### Монорепозиторий (Yarn Workspaces):

```
ai-finance-app/
├── .rules/                     # 📚 ВСЯ ДОКУМЕНТАЦИЯ ЗДЕСЬ
├── apps/
│   ├── frontend/               # React + Vite + PWA
│   │   └── src/
│   │       ├── app/            # App слой (конфиг, роутинг, store)
│   │       ├── pages/          # Pages слой (страницы)
│   │       ├── widgets/        # Widgets слой (сложные блоки)
│   │       ├── features/       # Features слой (фичи)
│   │       ├── entities/       # Entities слой (бизнес-сущности)
│   │       └── shared/         # Shared слой (общее)
│   └── backend/                # NestJS API
│       └── src/
│           ├── modules/
│           │   ├── ai/         # AI модуль (OpenAI интеграция)
│           │   ├── transactions/
│           │   ├── accounts/
│           │   └── categories/
│           ├── app.module.ts
│           └── main.ts
├── packages/
│   ├── ui/                     # UI библиотека (Headless UI + Tailwind)
│   └── shared/                 # Общие типы и константы
├── README.md                   # Краткое описание + ссылки на .rules/
└── package.json                # Yarn workspaces config
```

### Feature-Sliced Design (FSD) - Frontend:

- **app/** - Инициализация (providers, routing, global styles)
- **pages/** - Страницы приложения (композиция виджето��)
- **widgets/** - Крупные самостоятельные блоки (header, sidebar, etc)
- **features/** - Части функционала (add-transaction, auth, etc)
- **entities/** - Бизнес-сущности (transaction, account, category)
- **shared/** - Переиспользуемый код (ui, api, lib, config)

### Ключевые технологии:

**Frontend:**
- React 18 + TypeScript
- Vite (сборщик) + PWA плагин
- RTK Query (API запросы)
- React Router (роутинг)
- Tailwind CSS (стили)
- Headless UI (компоненты)
- FSD (архитектура)

**Backend:**
- NestJS (фреймворк)
- TypeScript
- OpenAI GPT-4o-mini (AI парсинг)
- Swagger (документация API)
- class-validator (валидация)
- class-transformer (трансформация)

**Monorepo:**
- Yarn Workspaces
- Shared packages (ui, shared)

### API Endpoints (Backend):

```
POST   /api/ai/parse              # AI парсинг текста
GET    /api/transactions          # Все транзакции
POST   /api/transactions          # Создать транзакцию
GET    /api/transactions/:id      # Транзакция по ID
DELETE /api/transactions/:id      # Удалить транзакцию
GET    /api/accounts              # Все счета
POST   /api/accounts              # Создать счёт
GET    /api/accounts/:id          # Счёт по ID
GET    /api/categories            # Все категории
POST   /api/categories            # Создать категорию
GET    /api/docs                  # Swagger UI
```

---

## 🎨 Стандарты кодирования

### TypeScript:
- Strict mode enabled
- Явная типизация всех функций
- Интерфейсы для объектов
- Enums для констант

### React:
- Functional components
- Custom hooks для логики
- Props деструктуризация
- TypeScript для всех компонентов

### NestJS:
- Модульная архитектура
- Dependency Injection
- DTO для валидации
- Swagger декораторы

### Git:
- Осмысленные commit messages
- Ветки по feature
- Code review перед merge

---

## 🔧 Команды разработки

```bash
# Установка
yarn install

# Разработка
yarn dev              # Всё сразу
yarn dev:frontend     # Только frontend
yarn dev:backend      # Только backend

# Сборка
yarn build            # Всё
yarn build:frontend   # Только frontend
yarn build:backend    # Только backend

# Линтинг
yarn lint             # Всё
yarn lint:frontend    # Только frontend
yarn lint:backend     # Только backend

# Очистка
yarn clean            # Удалить node_modules везде
```

---

## 📌 Важные файлы конфигурации

### Корневые:
- `package.json` - Workspaces, скрипты
- `.gitignore` - Git ignore
- `.prettierrc` - Форматирование кода
- `.nvmrc` - Версия Node.js

### Frontend:
- `vite.config.ts` - Vite + PWA
- `tsconfig.json` - TypeScript
- `tailwind.config.js` - Tailwind CSS
- `eslint.config.js` - ESLint

### Backend:
- `nest-cli.json` - NestJS CLI
- `tsconfig.json` - TypeScript (decorators!)
- `.eslintrc.js` - ESLint
- `.env` - Переменные окружения

---

## ✅ Чек-лист при изменениях

Перед commit:
- [ ] Код проходит линтинг
- [ ] TypeScript компилируется без ошибок
- [ ] Документация обновлена в `.rules/`
- [ ] README.md актуален (если нужно)
- [ ] Commit message понятен

При добавлении функционала:
- [ ] Обновлён соответствующий файл в `.rules/`
- [ ] API endpoints задокументированы (если добавлены)
- [ ] Типы обновлены в `packages/shared`
- [ ] UI компоненты добавлены в `packages/ui` (если нужно)

---

## 🚨 Критически важно

1. **OPENAI_API_KEY** должен быть в `apps/backend/.env`
2. **Документация** ТОЛЬКО в `.rules/`
3. **FSD архитектура** строго соблюдается во frontend
4. **TypeScript** - строгий режим везде
5. **Yarn** - единственный пакетный менеджер

---

**Последнее обновление**: 25 декабря 2024
**Версия инструкций**: 1.0.0

