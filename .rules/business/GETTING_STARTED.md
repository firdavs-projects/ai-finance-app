# AI Finance - Инструкции по запуску

## ✅ Статус установки

Базовая структура монорепозитория создана успешно!

### Установленные компоненты:
- ✅ Монорепозиторий с npm workspaces
- ✅ Frontend (React + Vite + PWA + FSD)
- ✅ Backend (NestJS)
- ✅ UI библиотека (Headless UI + Tailwind)
- ✅ Shared типы
- ✅ ESLint + Prettier
- ✅ Зависимости установлены (899 пакетов)

---

## 🚀 Запуск приложения

### 1. Настройка окружения

Скопируйте файлы окружения:

```bash
cd /Users/firdavsabdulloev/Alif/ai-app

# Frontend
cp apps/frontend/.env.example apps/frontend/.env

# Backend
cp apps/backend/.env.example apps/backend/.env
```

### 2. Настройте OpenAI API ключ

Отредактируйте `apps/backend/.env`:

```env
OPENAI_API_KEY=sk-your-actual-api-key-here
```

### 3. Запуск приложений

**Вариант 1: Запуск всего сразу (из корня)**

```bash
# Оба сервиса вместе
yarn dev

# Или по отдельности
yarn dev:frontend   # Frontend на порту 5173
yarn dev:backend    # Backend на порту 3001
```

**Вариант 2: Запуск отдельно**

```bash
# Frontend
cd apps/frontend
yarn dev

# Backend (в другом терминале)
cd apps/backend
yarn dev
```

---

## 📋 Доступные команды

### Корневой уровень

```bash
yarn dev:frontend       # Запуск frontend
yarn dev:backend        # Запуск backend
yarn build              # Сборка всех проектов
yarn lint               # Линтинг всех проектов
yarn clean              # Очистка node_modules
```

### Frontend (apps/frontend)

```bash
yarn dev                # Dev сервер (http://localhost:5173)
yarn build              # Production сборка
yarn preview            # Превью production сборки
yarn lint               # ESLint проверка
```

### Backend (apps/backend)

```bash
yarn dev                # Dev сервер с hot reload (http://localhost:3001)
yarn build              # Production сборка
yarn start              # Запуск production
yarn lint               # ESLint проверка
```

---

## 🌐 URL приложения

После запуска:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001/api
- **Swagger Docs**: http://localhost:3001/api/docs

---

## 🔧 Устранение проблем

### Если yarn install падает с ошибками прав доступа

```bash
sudo chown -R $(whoami) "/Users/firdavsabdulloev/.yarn"
yarn install
```

### Если порты заняты

Отредактируйте:
- Frontend: `apps/frontend/vite.config.ts` (измените порт в server.port)
- Backend: `apps/backend/.env` (измените PORT=3001)

### Проверка зависимостей

```bash
# Посмотреть установленные пакеты
yarn workspaces list

# Переустановка
yarn clean
yarn install
```

---

## 📁 Структура проекта

```
ai-finance-app/
├── apps/
│   ├── frontend/          # React приложение
│   │   ├── src/
│   │   │   ├── app/       # Конфигурация приложения
│   │   │   ├── pages/     # Страницы
│   │   │   ├── widgets/   # Виджеты (layouts, ai-input)
│   │   │   ├── features/  # Фичи
│   │   │   ├── entities/  # Сущности
│   │   │   └── shared/    # Общие модули
│   │   └── package.json
│   └── backend/           # NestJS API
│       ├── src/
│       │   ├── modules/   # Модули (transactions, accounts, categories, ai)
│       │   ├── app.module.ts
│       │   └── main.ts
│       └── package.json
├── packages/
│   ├── ui/                # UI компоненты
│   └── shared/            # Общие типы
└── package.json           # Корневой package.json
```

---

## 🤖 Использование AI функции

После запуска в нижней части экрана будет поле ввода с иконкой 🤖.

**Примеры запросов:**

```
американо 22сом, чизкейк 15сом
выпил кофе за 25 сомов
такси 50 сомов
зарплата 50000 сомов
```

AI автоматически:
- Распознает транзакции
- Определит категории
- Извлечёт суммы
- Создаст записи

---

## 📝 Следующие шаги

1. ✅ Запустите frontend: `yarn dev:frontend`
2. ✅ Запустите backend: `yarn dev:backend`
3. 🔑 Добавьте OpenAI API ключ в `apps/backend/.env`
4. 🌐 Откройте http://localhost:5173
5. 🧪 Протестируйте AI input в нижней части экрана

---

## 🐛 Известные предупреждения

При установке могут быть предупреждения:
- `vulnerabilities` - можно исправить через `yarn audit`
- Deprecated пакеты - не критично для разработки

---

## 💡 Дополнительная информация

- Используется Feature-Sliced Design (FSD) архитектура
- PWA поддержка (приложение можно установить на телефон)
- Tailwind CSS для стилей
- RTK Query для API запросов
- OpenAI GPT-4o-mini для обработки текста

Удачи в разработке! 🚀

