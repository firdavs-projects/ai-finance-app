# 🎯 Как использовать AI парсер для транзакций

**Статус**: ✅ Готово к использованию  
**Дата**: 25 декабря 2025

---

## 📱 Для пользователей

### Где найти AI виджет

AI виджет расположен **внизу экрана** на всех страницах приложения, над нижней навигацией.

### Как создать транзакцию

1. **Введите текст** в поле AI виджета:
   ```
   американо 22смн и чизкейк 15смн
   ```

2. **Отправьте** одним из способов:
   - Нажмите кнопку отправки (✈️)
   - Или нажмите **Ctrl+Enter** (Windows/Linux) / **Cmd+Enter** (Mac)

3. **Получите результат**:
   - ✅ **Зелёное уведомление**: "Создано 2 транзакций"
   - ❌ **Красное уведомление**: Ошибка (попробуйте переформулировать)
   - ❓ **Синее уведомление**: AI просит уточнение

### Примеры того, что можно писать

**Простая покупка**:
```
кофе 25смн
```

**Несколько покупок сразу**:
```
американо 22смн, чизкейк 15смн, вода 5смн
```

**С указанием места**:
```
в Do.Bro купил кофе 22смн
```

**Доход**:
```
зарплата 5000 смн
```

**С указанием счета** ✨ НОВОЕ:
```
хлеб 25с наличными
кофе 30смн картой
такси 50 на карту
пиво 15 наличкой
```

**Разные валюты**:
```
кофе 150 рублей
```
```
обед 10 долларов
```

### Поддерживаемые валюты

| Текст | Валюта |
|-------|--------|
| смн, сомони, с | TJS (Таджикский сомони) |
| рубль, руб | RUB (Российский рубль) |
| доллар, $ | USD (Доллар США) |
| тенге | KZT (Казахстанский тенге) |

### Что делает AI автоматически

1. **Распознаёт суммы** - извлекает числа и валюты
2. **Определяет категории** - подбирает подходящую категорию (Кафе, Продукты, etc.)
3. **Определяет тип** - расход или доход
4. **Распознаёт счет** ✨ НОВОЕ - автоматически определяет счет по ключевым словам:
   - "наличными", "наличкой", "кешем" → Наличные
   - "картой", "на карту", "card" → Банковская карта
   - Если не указано → использует первый (дефолтный) счет
5. **Создаёт транзакции** - сохраняет с правильным счетом и категорией
6. **Обновляет баланс** - автоматически пересчитывает остаток

### Примеры с разными счетами

| Что вы пишете | Куда сохранится |
|---------------|-----------------|
| "хлеб 25с наличными" | Счет "Наличные" |
| "кофе 30смн картой" | Счет "Банковская карта" |
| "такси 50 на карту" | Счет "Банковская карта" |
| "пиво 15 наличкой" | Счет "Наличные" |
| "продукты 200" | Дефолтный счет |

---

## 💻 Для разработчиков

### Запуск приложения

**Backend**:
```bash
cd /Users/firdavsabdulloev/Alif/ai-app
pnpm dev:backend
```

**Frontend**:
```bash
cd /Users/firdavsabdulloev/Alif/ai-app
pnpm dev:frontend
```

**Оба сразу**:
```bash
pnpm dev
```

### Проверка работы

1. Откройте http://localhost:5173
2. Убедитесь, что MongoDB подключена (проверьте логи backend)
3. Убедитесь, что OpenAI API Key действителен
4. Введите тестовый текст в AI виджет
5. Проверьте создание транзакций

### Отладка

**Смотреть логи AI**:
```bash
# Backend логи покажут запросы к OpenAI
tail -f logs/backend.log
```

**Проверить ответ AI в DevTools**:
1. Откройте Chrome DevTools (F12)
2. Вкладка Network
3. Отправьте текст в AI виджет
4. Найдите запрос к `/api/ai/parse`
5. Посмотрите Response

### Структура кода

**Frontend**:
- `/apps/frontend/src/entities/ai/` - AI entity (RTK Query)
- `/apps/frontend/src/widgets/ai-input/` - UI компонент виджета

**Backend**:
- `/apps/backend/src/modules/ai/` - AI модуль (OpenAI интеграция)

### API Endpoint

```typescript
POST /api/ai/parse

Request:
{
  text: string       // "американо 22смн"
  accountId?: string // опционально
}

Response (Success):
{
  success: true
  transactions: Transaction[]
  message: "Создано 2 транзакций"
}

Response (Error):
{
  success: false
  error: string
}

Response (Clarification):
{
  success: false
  needsClarification: true
  question: string
}
```

---

## 🐛 Troubleshooting

### Проблема: "Не удалось распознать текст"

**Решения**:
1. Попробуйте переформулировать более чётко
2. Укажите сумму явно с валютой: "22смн" вместо "22"
3. Используйте простые фразы: "кофе 22смн" вместо сложных предложений

### Проблема: Неправильная категория

**Решение**:
- После создания транзакции можно вручную изменить категорию
- AI учится на доступных категориях в системе
- Добавьте нужную категорию в настройках, если её нет

### Проблема: Не создаются транзакции

**Проверьте**:
1. ✅ Backend запущен (http://localhost:3001)
2. ✅ MongoDB подключена
3. ✅ OpenAI API Key действителен и имеет кредиты
4. ✅ В системе есть хотя бы один счёт

### Проблема: Ошибка 500 Internal Server Error

**Проверьте**:
1. Логи backend на наличие ошибок
2. Переменные окружения в `.env`
3. Подключение к MongoDB Atlas
4. Лимиты OpenAI API

---

## 📊 Известные ограничения

1. **Язык**: Лучше работает на русском языке
2. **Сложность**: Простые фразы работают лучше сложных
3. **Контекст**: AI не помнит предыдущие сообщения (пока)
4. **Скорость**: Зависит от скорости ответа OpenAI (обычно 1-3 секунды)

---

## 🚀 Следующие улучшения

См. раздел "Следующие шаги" в `.rules/ai-reports/AI_PARSER_INTEGRATION.md`

---

**Вопросы?** Смотрите полную документацию: `.rules/ai-reports/AI_PARSER_INTEGRATION.md`

