# AI Finance 💰🤖

Умное приложение для учёта личных финансов с возможностями ИИ в стиле ZenMoney.

## Особенности

- 📝 **Ввод расходов на естественном языке** — просто напишите "американо 22сом, чизкейк 15сом" и AI создаст транзакции
- 💳 **Управление счетами** — наличные, карты, банковские счета
- 📊 **Аналитика** — отслеживайте куда уходят деньги
- 📱 **PWA** — устанавливается как приложение на телефон
- 🎨 **Современный UI** — в стиле ZenMoney

## Структура проекта

```
ai-finance-app/
├── apps/
│   ├── frontend/          # React + Vite + PWA (FSD архитектура)
│   └── backend/           # NestJS API
├── packages/
│   ├── ui/                # Shared UI компоненты (Headless UI + Tailwind)
│   └── shared/            # Общие типы и константы
├── package.json           # Root package.json
└── pnpm-workspace.yaml    # pnpm workspaces config
```

## Технологии

### Frontend
- React 18
- Vite + PWA
- RTK Query
- Tailwind CSS
- Feature-Sliced Design (FSD)
- Headless UI

### Backend
- NestJS
- OpenAI GPT-4o-mini
- Swagger

## Начало работы

### Требования
- Node.js >= 20
- yarn >= 4.0

### Установка

```bash
# Установка зависимостей
yarn install

# Копирование env файлов
cp apps/backend/.env.example apps/backend/.env
cp apps/frontend/.env.example apps/frontend/.env

# Добавьте ваш OpenAI API ключ в apps/backend/.env
```

### Запуск

```bash
# Запуск всего (frontend + backend)
yarn dev

# Или по отдельности
yarn dev:frontend   # http://localhost:5173
yarn dev:backend    # http://localhost:3001
```

### API Документация

После запуска бэкенда Swagger доступен по адресу:
http://localhost:3001/api/docs

## Пример использования AI

Введите текст в поле ввода внизу экрана:

```
"Выпил американо за 22 сом и съел чизкейк за 15 сом"
```

AI автоматически:
1. Распознает 2 транзакции
2. Определит категорию "Кафе и рестораны"
3. Создаст записи с правильными суммами

## Лицензия

MIT

